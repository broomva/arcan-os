{
  "name": "@agent-os/engine-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@agent-os/core": "workspace:*",
    "@agent-os/event-store": "workspace:*",
    "@agent-os/run-manager": "workspace:*",
    "@agent-os/tool-kernel": "workspace:*",
    "ai": "^4.3.0",
    "@ai-sdk/anthropic": "^1.2.0",
    "@ai-sdk/openai": "^1.3.0"
  }
}
