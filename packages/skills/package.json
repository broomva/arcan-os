{
  "name": "@agent-os/skills",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "LICENSE"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/broomva/agent-os.git",
    "directory": "packages/skills"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun && tsc --emitDeclarationOnly --outDir dist",
    "dev": "tsc --watch --emitDeclarationOnly --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.8.0",
    "@agent-os/tsconfig": "workspace:*"
  },
  "dependencies": {
    "@agent-os/core": "workspace:*",
    "yaml": "^2.4.0"
  }
}
